const hello = ()=> {
    console.log("Hello, World!");
}

export default hello;
/*crea otra carpeta con la extecion mjs y en esa usa el 
 import hello from './module '*/

 // Despues solo llama la function hello

 hello();